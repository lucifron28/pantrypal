// Ingredient List Widget
import 'package:flutter/material.dart';

class IngredientList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Ingredient List Widget'));
  }
}
